from setuptools import setup, find_packages

setup(name='AM-powder',
      version='0.0.0',
      url='https://github.com/tlziev/AM-powder',
      description="",
      author='Tracey L Ziev',
      packages=find_packages(),
      package_data={
      },
      install_requires=[
      ],
      )
